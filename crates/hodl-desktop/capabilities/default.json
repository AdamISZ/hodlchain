{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for hodlcoin wallet's main window",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
